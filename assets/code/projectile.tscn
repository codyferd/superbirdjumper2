[gd_scene load_steps=2 format=3 uid="uid://bdkjmk1idke70"]

[sub_resource type="GDScript" id="GDScript_38v21"]
script/source = "extends Sprite2D

var velocity = Vector2.ZERO
var speed = 400  # You can adjust this

func shoot(direction: Vector2):
	velocity = direction.normalized() * speed

func _process(delta):
	position += velocity * delta

	# Auto-remove if offscreen (you can adjust this as needed)
	if position.y < -100 or position.y > 1200 or position.x < -100 or position.x > 2000:
		queue_free()
"

[node name="projectile" type="Sprite2D"]
script = SubResource("GDScript_38v21")
